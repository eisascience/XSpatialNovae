# Optional dependencies for histology/whole-slide image workflows
# ONLY install these if you need to work with histology images
#
# Installation:
#   pip install -r requirements-histology.txt
#   OR
#   uv pip install -r requirements-histology.txt
#   OR
#   pip install -e ".[histology]"
#
# Platform-specific prerequisites:
#   - macOS: brew install openslide
#   - Ubuntu/Debian: sudo apt-get install openslide-tools
#   - Windows: Download OpenSlide binaries from https://openslide.org/download/

openslide-python>=1.3.0
pillow>=10.0.0
scikit-image>=0.21.0
